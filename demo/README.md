# CLJS Demo

Standalone ClojureScript demo app for jon-nested-menu.

This demo is separate from the library bundle and consumes the library namespace.

## Run

```bash
cd demo
npm install
npm run start
```

Open http://localhost:8080.

## Build

```bash
npm run build
```
