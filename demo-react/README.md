# React Demo

Standalone React + MUI demo app for jon-nested-menu.

This demo is separate from the CLJS library bundle.
It consumes the local npm package (`jon-nested-menu` via `file:..`).

## Run

```bash
cd demo-react
npm install
npm run dev
```

## Build

```bash
npm run build
npm run preview
```
